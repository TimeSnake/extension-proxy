main: de.timesnake.extension.proxy.main.ExProxy
version: ${version}
name: extension-proxy
author: timesnake
depends: [ basic-proxy ]

commands:
  server:
  lobby:
  hub:
  leave:
  menu:

  msg:
  tell:
  message:
  pm:
  r:
  msgx:
  service:
  mail:
  broadcast:
  warn:

  ping:
  bugfind:
     